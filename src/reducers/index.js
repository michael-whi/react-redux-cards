export function mainReducer(state={}, action) => {
    switch(action.type){
        case "SAVE_POS":
            return null
        case "SAVE_TEXT":
            return null    
        default:
            return state
    }
}